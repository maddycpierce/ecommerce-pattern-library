title: "Footer"
description: |
  The footer is to be placed at the bottom of every page of the website.

patterns:
  footer:
    fields:
      - name: text
        type: string
        example: "WANT AN EXTRA 20% OFF? Refer a friend."
      - name: button_text
        type: string
        example: "Let us know how we're doing. Feedback"
